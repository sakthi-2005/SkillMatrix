import { createSlice } from "@reduxjs/toolkit";

const userSlice = createSlice({
    name:'user_id',
    initialState:null,
    reducers:{
        
    }
});

export default userSlice.reducer;

//export {  } = userSlice.actions; // reducers inside 